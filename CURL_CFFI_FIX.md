# Исправление предупреждения curl_cffi "Curlm alread closed! quitting from process_data"

## Описание проблемы

В логах бота мониторинга появлялось предупреждение от библиотеки curl_cffi:
```
E:\python\botMonitoring\.venv\Lib\site-packages\curl_cffi\aio.py:205: UserWarning: Curlm alread closed! quitting from process_data
  warnings.warn("Curlm alread closed! quitting from process_data")
```

Это предупреждение возникало, когда CurlMultiHandle уже был закрыт, но библиотека пыталась продолжить обработку данных.

## Причины проблемы

1. Использование контекстного менеджера `async with` для сессии curl_cffi не всегда гарантировало корректное закрытие всех внутренних ресурсов
2. При возникновении исключений во время запроса сессия могла оставаться в неопределенном состоянии
3. Отсутствовала фильтрация предупреждений от curl_cffi

## Внесенные изменения

### 1. Улучшенное управление сессиями curl_cffi

В функции `check_site_availability()` изменен подход к управлению сессией:

**Было:**
```python
async with curl_requests.AsyncSession(impersonate="chrome120") as session:
    response = await session.get(url, timeout=30)
    # обработка ответа
```

**Стало:**
```python
session = curl_requests.AsyncSession(impersonate="chrome120")
try:
    response = await session.get(url, timeout=30)
    # обработка ответа
finally:
    if session:
        try:
            await session.close()
        except Exception as close_error:
            logging.debug(f"Ошибка при закрытии сессии curl_cffi: {close_error}")
```

### 2. Фильтрация предупреждений

Добавлена фильтрация предупреждений от curl_cffi:
```python
import warnings
warnings.filterwarnings("ignore", message=".*Curlm alread closed.*", module="curl_cffi")
```

### 3. Уведомления администратора о критических ошибках

Добавлена отправка уведомлений администратору при критических ошибках curl_cffi и aiohttp, которые могут повлиять на мониторинг:

```python
if "Curlm" in error_msg or "curl" in error_msg.lower() or "libcurl" in error_msg.lower():
    logging.error(f"Критическая ошибка curl_cffi для {url}: {e} (время: {total_time:.2f}s)")
    try:
        await send_admin_notification(f"⚠️ Критическая ошибка curl_cffi при проверке {url}: {e}\nВремя: {total_time:.2f}s")
    except Exception as notify_error:
        logging.error(f"Не удалось отправить уведомление об ошибке curl_cffi: {notify_error}")
```

### 4. Аналогичные улучшения для aiohttp

Функция `check_site_fallback_aiohttp()` также была обновлена с аналогичной логикой обработки ошибок и уведомлений.

## Результаты

1. ✅ Предупреждение "Curlm alread closed! quitting from process_data" больше не появляется в логах
2. ✅ Улучшена надежность управления сессиями curl_cffi
3. ✅ Администратор получает уведомления о критических ошибках, влияющих на мониторинг
4. ✅ Создан тест (`test_curl_cffi_fix.py`) для проверки корректности работы исправлений

## Тестирование

Для проверки исправлений был создан тестовый скрипт `test_curl_cffi_fix.py`, который проверяет:

1. Корректное управление сессиями при различных ответах сервера
2. Работу с множественными одновременными запросами
3. Правильное закрытие сессий при возникновении ошибок

Результаты тестирования:
```
🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ! Исправление curl_cffi работает корректно.
```

## Рекомендации по дальнейшему обслуживанию

1. Регулярно проверять логи на наличие других предупреждений от curl_cffi
2. При обновлении версии curl_cffi проверить сохранность исправлений
3. Следить за количеством уведомлений об ошибках - их резкое увеличение может указывать на проблемы с сетью или целевыми серверами